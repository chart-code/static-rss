{
  "dependencies": {
    "node-fetch": "^2.6.1",
    "rss-parser": "^3.12.0",
    "sanitize-filename": "^1.6.3",
    "scrape-stl": "^1.0.3",
    "xml2json": "^0.12.0"
  },
  "scripts": {
    "pub": "cd .. && rsync -a --omit-dir-times --no-perms --exclude node_modules --exclude .git --exclude public/items.json --exclude bin/cache/*.xml static-rss/ demo@roadtolarissa.com:static-rss/"
  }
}
